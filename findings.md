# 研究笔记（工程约束）：**不是**项目进度的唯一真相源

仓库里 **`task_plan.md`、`progress.md`、`本文件`** 以及散落各处的说明，都**可能被某次会话写过但没续更**。  
判断「现在做到哪了」请优先看：

- **`git log`**（最近改了什么）
- **`OpenMV_flash/`**（当前约定要烧的三件东西）
- **根目录 `line_follow_params.json`**（仿真 / Webots 主参数）
- **`Webots/controllers/line_follow_transfer/`**、**`CVpart/main/main_webots_aligned.py`**（算法与控制器对齐关系）

---

## 赛事规则 PDF — 已入库：`rule.pdf`（仓库根目录）

来源：**《工程竞技类机器人赛项人形竞技全能项目竞赛规则》**（2025 年 2 月版，中国机器人大赛暨 RoboCup 中国赛）。  
以下摘录自 PDF 文本层（**非臆造**）；页码以 PDF 阅读器为准，章节以原文「六、七」等为准。

### 与视觉 / 二维码直接相关的官方表述（摘录）

**背景（第一节要义）**  
机器人须用传感器识别**赛道、二维码、障碍物**等，并实现闭环控制。

**6.2 比赛器材说明 —（一）二维码**

1. 二维码为数字 **1～6**，对应 6 个规定动作序号；可白纸打印，尺寸为 **5 cm×5 cm** 正方形；图案见附录。  
2. 赛道中摆放 **3 个**二维码：**第 1 个**在起跑线**前方 30 cm**；**第 2 个**在**直道中间**；**第 3 个**在**弯道中间**（见图 1）。  
3. 赛前由队员从 1～6 **随机抽取**二维码；赛时可用透明胶带粘贴在场地图上。

**7.2 比赛流程 — 二维码抽签**  
参赛队员从数字 1～6 的**六张二维码中抽取 3 张**作为本场动作二维码。

**7.3 比赛任务 —（一）任务 2**  
行进中须识别 **3 处**二维码并完成对应动作：

| 序号 | 动作要求 |
|------|-----------|
| 1 | 举起左手，维持 **3 s 以上** |
| 2 | 举起右手，维持 **3 s 以上** |
| 3 | 抬起左腿，维持 **3 s 以上** |
| 4 | 抬起右腿，维持 **3 s 以上** |
| 5 | 举起双手，维持 **3 s 以上** |
| 6 | 左右摇头，维持 **3 s 以上** |

**7.3 —（二）评分 — 二维码项目**  
共 3 处二维码；每处**正确识别并完成指定动作**得 **20 分**（单场最多 60 分该项）。

**场地尺寸（与提前量估计相关）**  
场地 **3660 mm×2440 mm**；环形赛道全长约 **6140 mm**，赛道宽 **350 mm**（用于估算「离码多远仍应在视野内」）。

### 待你自己对照 PDF 复核的细节

- 附录中**具体二维码图案**（提取文本未包含图像，需打开 PDF 「附录」看图）  
- 「限宽门 240 mm」等与**机位、高度**相关的布置以 **图 1** 为准

### 与当前固件的差距（工程判断，非规则条文）

- 规则要求场上 **3 次**扫码 + **主控执行 3 s 动作**；**`OpenMV_flash/main.py` 当前无 `find_qrcodes`**，**不满足**正式比赛对「行进中识别二维码」的完整性。  
- **`qr_uart_test.py`** 已覆盖 **1～6 与 UART uint8 1～6**，与规则数字一致；需 **并入刷机主程序** 并与主控**动作时长（≥3 s）**对齐。

### 规则全文（15 页文本层）对照 — 本仓库**已做** vs **未做/弱**

> 依据：`rule.pdf` 全文抽取通读。附录中的**二维码位图**在 PDF 里是图，文本层只有数字 1–6 占位，**生成赛用码需照 PDF 附录图像**。

| 规则要求（概括） | 本仓库现状 | 缺口或风险 |
|------------------|------------|------------|
| 逆时针沿 **6140 mm** 环形赛道（宽 **350 mm**）走完一圈 | Webots + `line_follow_transfer` 与 `OpenMV_flash/main.py` **巡线管线对齐**；输出误差不多意义上「跟线」 | **真机双足步态、整圈闭环**不在本仓库；需主控与机械 |
| **3 处**二维码（**5 cm×5 cm**；第 1 在起跑线前 **30 cm**；第 2 直道；第 3 弯位）；赛前抽 **3 张** 1–6 | **`OpenMV_flash/main.py` 无二维码**；仅 **`qr_uart_test.py`可识别 1–6** | **必须在刷机主程序内集成**识别+P1 发码+与巡线分时；**三处不重复动作**需策略（或主控记分） |
| 识别后做对应动作且 **每种维持 ≥3 s** | OpenMV 只发 **uint8 指令**（`qr_uart_test` 还有 **5 s** 发一次节流） | **≥3 s 动作持续时间**须 **主控保证**；视觉侧节流是否与「允许重复读同一码」冲突需联调 |
| 跨越 **红色包裹** 木条（10×20 mm，竖 20/平 10）；碰移木条则 **越障 0 分** | `main_webots_aligned` 里 **红块检测默认关**（灰度图，需 `red_detect_on_grayscale` 等才开）；Webots 侧重巡线 | **红色障碍与跨越策略**主要在本届仍是**步态/里程计**；视觉若要做「接近木条」辅助需单独开 **RGB 或的红启发** |
| **限宽门最窄 240 mm**（场地图注） | 未见独立「限宽门」视觉模块 | 若需：可能要 **额外 ROI/宽度检测** 或与线宽逻辑结合 |
| 使用传感器识别 **赛道、二维码、障碍物** 并闭环 | **赛道**有；**二维码**仅在测试脚本；**障碍物**弱（红条） | 与规则文字相比：**障碍物/红条** 覆盖面不足 |
| 比赛时间 **≤4 min**、准备 **≤1 min**、两轮取最好 | 无计时计分软件 | 队务/场边 |
| 行进速度分公式、二维码 **20×3**、越障 **60**、扣分/终局/零分条款 | 无自动算分 | 队务理解即可 |
| 资格认证 WORD、队名标识、QQ 群等 | 无 | 行政 |
| 机器人结构 **DOF≥13**、尺寸 **高<450 mm** 等 | 无 | 机械 |
| **禁止赛中无线/有线遥控**；程序 **赛前一次烧录**、裁判可要求重启 | 本仓库是 **本体自主 UART 协议**，不实现遥控 | 架构需队里自检合规 |
| 附加分：机械/电路**自主设计**与资料 | 无 | 队务 |

**关于文件名**：规则曾用长中文名时，部分工具对路径/编码敏感；现 **`rule.pdf`** 在仓库根目录，已能稳定打开与检索。

---

## 二维码 — 工程侧现状（非规则摘录）

| 项 | 状态 |
|----|------|
| **`OpenMV_flash/main.py`（上场默认）** | **无** `find_qrcodes`；只有巡线 + 协议 V2 等 |
| **专项测试** | `CVpart/main/qr_uart_test.py`：白名单 `"1"~"6"`（与规则一致），UART 发 **uint8** `0x01~0x06`，**5 s** 节流，`lens_corr` + QVGA；**未**约束主控「动作 ≥3 s」 |
| **历史参考** | `main_test0.py`、`main0.py` 含 QR + 旧发送逻辑；**未**合并进 `OpenMV_flash` |
| **与赛场对齐** | 规则：**5 cm×5 cm** 码，**3 处**（含起跑线前 **30 cm**）；须并入刷机 `main.py` 并测 **距离 × 倾角 × 延迟**；主控须按号码做 **≥3 s** 动作方可得分 |

### 导师/自测关注点（工程指标，非规则）

- 大倾角、**~0.5 s 级**解码、较「最近」再 **~20 cm** 仍能检出 —— 需建测试表与失败样本，见 `task_plan.md` 阶段 C（**计划文件本身也可能过时**）。

---

## OpenMV 与 Webots

- 刷机 JSON：`OpenMV_flash/line_follow_params.json`（真机；`sim_opencv_distort: false` 等）
- 仿真 JSON：仓库根 `line_follow_params.json`（可开 `sim_opencv_distort` 等）
- Webots：**`D:\Webots\msys64\mingw64\bin\webots.exe`**（R2025a）；`scripts/run_shake_eval.py`、`auto_tune_webots_params.py` 可驱动批跑

## 相机标定（新头）

- 照片集：`calib_photos_manual_now/`；结果：`generated/camera_calibration_result_calib_photos_manual_now.json`（若 `generated/` 被 ignore，以提交记录与根 `line_follow_params` 为准）
- 已写入根 `line_follow_params`、`presets`、`OpenMV_flash/line_follow_params`（数值以当前 JSON 文件为准）
