; Per-controller Python (overrides Webots Preferences → General → Python command for this folder only).
; Edit COMMAND if your Python is elsewhere. This interpreter must have: cv2, numpy
; (pip install -r requirements-sim.txt).

[python]
COMMAND = C:/Users/Lenovo/AppData/Local/Programs/Python/Python313/python.exe
